# Changesets

Run `pnpm changeset` for user-facing package changes and commit the generated markdown file.
The release workflow maintains the version pull request and publishes packages after it is merged.
