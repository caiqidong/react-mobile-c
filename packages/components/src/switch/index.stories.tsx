import { Switch } from './index';

export default { component: Switch, tags: ['autodocs'], title: 'Components/Switch' };
export const Basic = { args: { 'aria-label': 'Notifications', defaultChecked: true } };
export const Loading = { args: { 'aria-label': 'Saving preference', loading: true } };
